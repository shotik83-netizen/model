# Конфигурация приложения

`config.json` содержит только общие настройки, перечень подрядчиков и ссылки на актуальные датированные модели.

`source-mapping.json` используется как шаблон. В файле модели каждого подрядчика хранится собственная карта вкладок, колонок и ячеек.
